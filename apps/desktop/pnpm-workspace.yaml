allowBuilds:
  esbuild: set this to true or false
# Allow esbuild's postinstall (verifies its platform binary); everything else
# stays script-blocked by pnpm's default policy.
onlyBuiltDependencies:
  - esbuild
